name: Greet User
on: 
   workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Custom Composite Action
        uses: ./.github/actions/my_repo_name/action.yaml
        with:
          name: "Utsav"